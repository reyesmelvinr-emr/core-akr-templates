{
  "version": 1,
  "hooks": {
    "agentStop": [
      {
        "type": "command",
        "bash": "VALIDATOR=\".github/skills/akr-docs/scripts/validate_documentation.py\"; if command -v python >/dev/null 2>&1; then PYTHON_CMD=python; elif command -v python3 >/dev/null 2>&1; then PYTHON_CMD=python3; else PYTHON_CMD=''; fi; if [ -n \"$PYTHON_CMD\" ]; then \"$PYTHON_CMD\" -c \"from pathlib import Path; Path('.akr/logs').mkdir(parents=True, exist_ok=True)\"; else mkdir -p .akr/logs; fi; if [ ! -f \"$VALIDATOR\" ]; then echo '{\"summary\": {\"note\": \"validate_documentation.py not found - run distribute-skill.yml to populate the distributed bundle\"}}' | tee .akr/logs/last-validation.json; exit 0; fi; if [ ! -f modules.yaml ]; then echo '{\"summary\": {\"note\": \"modules.yaml not found - skipping module-aware validation\"}}' | tee .akr/logs/last-validation.json; exit 0; fi; TIMESTAMP=$(if [ -n \"$PYTHON_CMD\" ]; then \"$PYTHON_CMD\" -c \"import datetime; print(datetime.datetime.now(datetime.timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ'))\"; else date -u +\"%Y-%m-%dT%H:%M:%SZ\"; fi 2>/dev/null || echo \"unavailable\"); echo \"{\\\"session_id\\\": \\\"akr-session-$TIMESTAMP\\\", \\\"steps_completed\\\": 0, \\\"last_confirmed_step\\\": 0, \\\"estimated_input_tokens\\\": 0, \\\"estimated_output_tokens\\\": 0, \\\"estimated_total_tokens\\\": 0, \\\"estimated_credits_usd\\\": 0.0, \\\"cache_hits\\\": 0, \\\"cache_misses\\\": 0, \\\"cache_savings_tokens_estimate\\\": 0, \\\"preflight_aborts\\\": 0, \\\"github_mcp_calls\\\": 0, \\\"session_end\\\": \\\"$TIMESTAMP\\\"}\" > .akr/logs/last-session-ledger.json; if [ -n \"$PYTHON_CMD\" ]; then VALIDATOR_PY=\"$PYTHON_CMD\"; else VALIDATOR_PY=python; fi; CHANGED_FILES=$(git diff --name-only --diff-filter=AM HEAD -- docs); if [ -n \"$CHANGED_FILES\" ]; then CHANGED_FILES=\"$CHANGED_FILES\" \"$VALIDATOR_PY\" \"$VALIDATOR\" --changed-files --output json --fail-on needs | tee .akr/logs/last-validation.json; else \"$VALIDATOR_PY\" \"$VALIDATOR\" --all docs/modules --output json --fail-on needs | tee .akr/logs/last-validation.json; fi",
        "timeoutSec": 60
      }
    ]
  }
}
