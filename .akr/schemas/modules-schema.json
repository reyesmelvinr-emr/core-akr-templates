{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://akr-templates.com/schemas/modules-schema.json",
  "title": "AKR Modules Manifest Schema",
  "description": "Schema for modules.yaml used by AKR ProposeGroupings and GenerateDocumentation workflows",
  "type": "object",
  "required": ["project", "modules", "database_objects", "unassigned"],
  "additionalProperties": false,
  "properties": {
    "project": {
      "type": "object",
      "required": [
        "name",
        "layer",
        "standards_version",
        "minimum_standards_version",
        "compliance_mode"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "layer": {
          "type": "string",
          "enum": ["UI", "API", "Database", "Integration", "Infrastructure", "Full-Stack"]
        },
        "standards_version": {
          "type": "string",
          "pattern": "^v?\\d+\\.\\d+\\.\\d+$"
        },
        "minimum_standards_version": {
          "type": "string",
          "pattern": "^v?\\d+\\.\\d+\\.\\d+$"
        },
        "compliance_mode": {
          "type": "string",
          "enum": ["pilot", "production"]
        }
      }
    },
    "modules": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name",
          "businessCapability",
          "feature",
          "project_type",
          "status",
          "max_files",
          "files",
          "doc_output"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[A-Za-z][A-Za-z0-9]*$"
          },
          "businessCapability": {
            "type": "string",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          },
          "feature": {
            "type": "string",
            "pattern": "^FN\\d{5}_US\\d{3,}$"
          },
          "project_type": {
            "type": "string",
            "enum": ["api-backend", "ui-component", "microservice", "general"]
          },
          "status": {
            "type": "string",
            "enum": ["draft", "review", "approved", "in-progress", "deprecated"]
          },
          "compliance_mode": {
            "type": "string",
            "enum": ["pilot", "production"]
          },
          "max_files": {
            "type": "integer",
            "minimum": 1,
            "maximum": 8
          },
          "files": {
            "type": "array",
            "minItems": 1,
            "maxItems": 8,
            "items": {
              "type": "string",
              "minLength": 1
            },
            "uniqueItems": true
          },
          "doc_output": {
            "type": "string",
            "pattern": "^docs/.+\\.md$"
          },
          "review_sheet": {
            "type": "string",
            "pattern": "^docs/.+\\.md$",
            "description": "Committed review artifact path, typically docs/modules/.akr/{project}_review.md"
          },
          "draft_output": {
            "type": "string",
            "pattern": "^docs/.+\\.md$",
            "description": "Committed draft artifact path, typically docs/modules/.akr/{module}_draft.md"
          },
          "last_reviewed_at": {
            "type": "string",
            "format": "date-time",
            "description": "ISO-8601 timestamp for the most recent human review checkpoint"
          },
          "review_mode": {
            "type": "string",
            "enum": ["full", "incremental"],
            "description": "Mode used for the latest review pass"
          },
          "notes": {
            "type": "string"
          },
          "ssg_pass4_source_reread": {
            "type": "boolean",
            "default": false,
            "description": "Authorizes targeted Service file re-read in SSG Pass 4 for modules where service-file comments are the primary business rule source. Requires rationale in notes field."
          },
          "ssg_pass3_source_reread": {
            "type": "boolean",
            "default": false,
            "description": "Authorizes targeted type file re-read in SSG Pass 3 for UI modules where complex generic types are not representable in the component hierarchy summary."
          }
        }
      }
    },
    "database_objects": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "type", "source", "doc_output"],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "type": {
            "type": "string",
            "enum": ["table", "view", "procedure", "function", "schema"]
          },
          "source": {
            "type": "string",
            "minLength": 1
          },
          "doc_output": {
            "type": "string",
            "pattern": "^docs/.+\\.md$"
          },
          "feature": {
            "type": "string",
            "pattern": "^FN\\d{5}_US\\d{3,}$"
          },
          "businessCapability": {
            "type": "string",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          }
        }
      }
    },
    "unassigned": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["path", "reason"],
        "additionalProperties": false,
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1
          },
          "reason": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
